# Configuration for Cora dataset training

dataset: Cora
num_epochs: 200
batch_size: 32
hidden_channels: 64
num_layers: 2
dropout: 0.5
lr: 0.01
weight_decay: 5e-4

# ScaleGNN specific
use_lcs: true
lcs_threshold: 0.1
num_hops: 2
