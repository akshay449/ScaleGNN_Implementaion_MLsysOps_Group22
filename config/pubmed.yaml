# Configuration for PubMed dataset training

dataset: PubMed
num_epochs: 200
batch_size: 64
hidden_channels: 128
num_layers: 3
dropout: 0.6
lr: 0.01
weight_decay: 5e-4

# ScaleGNN specific
use_lcs: true
lcs_threshold: 0.15
num_hops: 3
